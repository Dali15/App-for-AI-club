# Railway Configuration
# Save as railway.toml in root directory

[build]
builder = "dockerfile"
buildpacks = ["heroku/python"]

[env]
SECRET_KEY = { fromRandom = { length = 32 } }
DEBUG = "False"
ALLOWED_HOSTS = "*"
